# 236

# Definition for a binary tree node.
class TreeNode:
    def __init__(self, x):
        self.val = x
        self.left = None
        self.right = None

class Solution:
    def lowestCommonAncestor(self, root: 'TreeNode', p: 'TreeNode', q: 'TreeNode') -> 'TreeNode':
        if not root or root == p or root == q:
            return root

        left = self.lowestCommonAncestor(root.left, p, q)
        right = self.lowestCommonAncestor(root.right, p, q)

        if left and right:
            return root
        return left if left else right
    

    '''
     binary tree. It recursively traverses the tree, checking if the current node is equal to either of the target nodes or if the target nodes are found in its left or right subtrees. If both target nodes are found in different subtrees, the current node is the lowest common ancestor.
    
    '''